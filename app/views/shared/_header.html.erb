<nav class="navbar navbar-expand-md bg-white fixed-top">
<div class="container">
    <%= link_to "MEEaT JAPAN", root_path, class: "navbar-brand font-weight-bold main-logo" %>
    <button class="navbar-toggler collapsed" data-toggle="collapse" data-target="#navcol">
      <span class="h1">=</span>
    </button>
    <div id="navcol" class="collapse navbar-collapse">
      <% if !current_user_exists? %>
        <ul class="navbar-nav ml-auto text-dark">
          <li class="nav-item mr-5"><%= link_to "Sign Up/Sign In", new_foreigner_session_path, class: "nav-link font-weight-bold" %></li>
          <%= render 'shared/lang_select'%>
        </ul>
      <% elsif japanese? %>
        <%= render 'shared/logged_in_jpn_header'%>
      <% else foreigner? %>
        <%= render 'shared/logged_in_foreigner_header'%>
      <% end %>
    </div>
  </div>
</nav>
